# Task Completion Checklist
- Run relevant Gradle tasks (`./gradlew test`, `./gradlew :desktop:run`, or `./gradlew build`) to verify changes compile and tests pass.
- Confirm TODO.md is updated to reflect progress on tasks.
- Provide succinct change summary and mention any follow-up actions or manual steps.
- Suggest next steps or verification commands for the user.
